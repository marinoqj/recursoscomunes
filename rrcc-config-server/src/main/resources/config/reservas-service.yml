spring:
  application:
    name: reservas-service
  datasource:
    url: jdbc:mysql://localhost:3306/rrcc?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: rrcc
    password: rrcc
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
    show-sql: true
   
server:
  port: 8092
  
test:
  value: Hello from config server

  