spring:
  application:
    name: mantenimiento-service
  datasource:
    url: jdbc:p6spy:mysql://localhost:3306/rrcc?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: rrcc
    password: rrcc
    driver-class-name: com.p6spy.engine.spy.P6SpyDriver	
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
    show-sql: true
   
server:
  port: 8091
  
test:
  value: Hello from config server
  
logging:
  level:
    root: "warn"
    com.p6spy: "error"
  